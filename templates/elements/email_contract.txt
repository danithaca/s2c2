{% autoescape off %}When: {{ contract.display_event_range }}
Compensation: {% if not contract.is_favor %}${{ contract.price }} (${{ contract.hourly_rate }}/hour){% else %}$0 (favor exchange){% endif %}
Note: {{ contract.description|default:'- None -' }}{% endautoescape %}