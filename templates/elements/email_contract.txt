{% autoescape off %}When: {{ contract.display_event_range }}
Compensation: {% if contract.is_favor %}${{ contract.price }} (${{ contract.hourly_rate }}/hour){% else %}$0 (favor exchange: the requester agreed to repay the favor another time){% endif %}
Note: {{ contract.description|default:'- None -' }}{% endautoescape %}