{% load account_tags %}
You have confirmed the offer from {% user_display target_user %}. You can response to the email to contact {% user_display target_user %}. Or use the following contact:

{% include 'elements/email_user_info.txt' with user=target_user %}

{% if not contract.is_favor %}
You agreed to pay {% user_display target_user %} ${{ contract.price }}. Don't forget to pay after the service is delivered.
{% endif %}

Review the request here:

{{ site_url}}{{ contract.get_absolute_url }}
