{% load p2_tags %}
{% user-short-name contract.initiate_user %} updated request:

Payment: ${{ contract.price }}
{{ contract.description }}

Review details here:

{{ site_url }}{{ match.get_absolute_url }}
