You just created a request:

When: {{ contract.display_event_range }}
Compensation: ${{ contract.price }}
Note: {{ contract.description|default:'- None -' }}
Initial contacts: {{ contract.count_total_match }}

Review here:

{{ site_url}}{{ contract.get_absolute_url }}